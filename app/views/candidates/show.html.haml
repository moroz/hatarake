.row
  .medium-9.columns
    .card
      .row
        .medium-3.columns
          = avatar_for candidate, :avatar
        .medium-9.columns
          %h2.candidate__name= candidate.display_name
          %p
            = sex_age_label(candidate.sex, candidate.profile.age)
            = looking_for_work_label(candidate.profile.looking_for_work)
            %br/
            %span.candidate__profession= candidate.profession.local_name if candidate.profession.present?

          - if can? :edit, candidate
            = link_to t('.change_picture'), edit_avatar_path, class: 'button small'
            = link_to t('.change_data'), edit_candidate_profile_path, class: 'button small'

    .card
      .card-divider
        %h3= t('.skills')
      .card-section
        %div
          - if candidate.skill_items.any?
            %table
              %tbody#skill_items
                = render candidate.skill_items
          - else
            = t('.no_skills')
        - if can? :edit, candidate
          = link_to t('.edit_skills'), candidate_edit_skills_path, class: 'button small'

    .card
      .card-divider
        %h3= t('.education')
      .card-section
        %div
          - if candidate.education_items.any?
            %table
              %tbody
                = render candidate.education_items
          - else
            = t('.no_education')
        - if can? :edit, candidate
          = link_to t('.edit_education'), new_education_item_path, class: 'button small'

    .card
      .card-divider
        %h3= t('.work_experience')
      .card-section
        %div
          - if candidate.work_items.any?
            %table
              %tbody
                = render candidate.work_items
          - else
            = t('.no_work_experience') 
        - if can? :edit, candidate
          = link_to t('.edit_work'), new_work_item_path, class: 'button small'
