.card
  .card-divider
    %h4 CV
  .card-section
    - if candidate.resumes.any?
      %table.resumes
        %tbody
          = render candidate.resumes
    - if can? :update, candidate
      = link_to "Upload another CV", new_resume_path, class: 'button'
