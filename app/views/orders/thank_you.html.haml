%h1= t('.heading')

%p= t('.we_have_registered_your_payment')

- @order.cart_items.each do |item|
  %li
    = item.quantity
    &times;
    = item.product.local_name

= link_to t('.back_to_dashboard'), dashboard_path, class: 'button'
