:plain
  var elem = document.getElementById('error_explanation');
  elem.innerHTML = '#{j render 'shared/errors', object: @object}';
  window.scroll(0, elem.offsetTop);
  document.querySelector('input[type=submit]').disabled = false;
