- breadcrumb :jobs_abroad
%h2.text-center= t('.heading')

- show_advanced = params[:adv].present? && params[:adv] == '1'
.row
  = render partial: 'basic_search', locals: {hidden: show_advanced}
.row
  = render partial: 'advanced_search', locals: {hidden: !show_advanced}

.offers__popular_locations.text-center
  %h4= t('.popular_locations')
  - @popular_locations.each do |location|
    = render 'popular_location', location: location, link_param: :cid
- if can? :create, Offer
  .text-center
    = link_to (fa_icon 'plus', text: t('offers.add_new_offer')), new_offer_path, class: 'custom_button'
#offer-list
  = render 'offers'
