%h2.text-center= t('home.featured_companies.heading')
.row
  - cont_class = "large-#{@companies.size*3} columns collapse small-centered"
  %div{class: cont_class}  
    - @companies.each_with_index do |company, i|
      - if i == @companies.size - 1
        - classes = "medium-6 large-#{12 / @companies.size} float-center columns end centered"
      - else
        - classes = "medium-6 large-#{12 / @companies.size} float-center columns centered"
      %div{class: classes}
        = link_to company, title: company.name do
          = avatar_for company, size: :normal, class: 'float-center'
