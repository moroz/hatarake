!!!
%html{:lang => "en"}
  = render 'header'
  %body.home
    = render 'offcanvas'
    .off-canvas-content{'data-off-canvas-content': ''}
      = render 'topbar'
      = render 'titlebar'
      .home__row1
        .row.home__row1__text
          = yield :row1
      .container
        .row
          = render 'messages'
        = yield
        .row
          %hr/
          InJobs
      
    = render 'ga'
